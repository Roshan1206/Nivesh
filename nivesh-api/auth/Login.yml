info:
  name: Login
  type: http
  seq: 4

http:
  method: POST
  url: "{{gateway}}/auth/login"
  body:
    type: json
    data: |-
      {
        "email": "ananya.khanna9@gmail.com",
        "password": "Passw0rd@09"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: login user. provide access and refresh token
