info:
  name: Login
  type: http
  seq: 4

http:
  method: POST
  url: "{{auth}}/auth/login"
  body:
    type: json
    data: |-
      {
        "email": "test@gmail.com",
        "password": "Password@123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
