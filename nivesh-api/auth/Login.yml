info:
  name: Login
  type: http
  seq: 4

http:
  method: POST
  url: "{{gateway}}/auth/login"
  body:
    type: json
    data: |-
      {
        "email": "Roshan@gmail.com",
        "password": "Password@1234"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
