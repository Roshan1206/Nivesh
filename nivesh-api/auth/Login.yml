info:
  name: Login
  type: http
  seq: 4

http:
  method: POST
  url: "{{auth}}/auth/login"
  body:
    type: json
    data: |-
      {
        "email": "Roshan1@gmail.com",
        "password": "Password@1234"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: login user. provide access and refresh token
