info:
  name: Forgot password
  type: http
  seq: 3

http:
  method: PATCH
  url: http://localhost:8081/auth/forgot
  body:
    type: json
    data: |-
      {
        "email": "system@nvsh.com",
        "password": "Password@1234"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
