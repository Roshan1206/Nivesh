info:
  name: Health
  type: http
  seq: 3

http:
  method: GET
  url: "{{account}}/actuator/health"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
