info:
  name: Transaction start
  type: http
  seq: 2

http:
  method: POST
  url: "{{transaction}}/transactions/{{requestId}}/verify"
  body:
    type: text
    data: "770295"
  auth: inherit

runtime:
  variables:
    - name: requestId
      value: 81c577d5-07be-4846-aeb4-ca0ba03674a3

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
