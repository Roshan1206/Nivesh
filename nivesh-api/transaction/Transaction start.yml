info:
  name: Transaction start
  type: http
  seq: 2

http:
  method: POST
  url: "{{transaction}}/transactions/{{requestId}}/verify"
  body:
    type: text
    data: "155847"
  auth: inherit

runtime:
  variables:
    - name: requestId
      value: a341f0cf-6998-4282-9ba9-5433a9443f63

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
