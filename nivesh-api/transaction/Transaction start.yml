info:
  name: Transaction start
  type: http
  seq: 2

http:
  method: POST
  url: "{{transaction}}/transactions/{{requestId}}/verify"
  body:
    type: text
    data: "813286"
  auth: inherit

runtime:
  variables:
    - name: requestId
      value: 96480423-a9a6-4cb1-88dd-23adc4d12752

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
