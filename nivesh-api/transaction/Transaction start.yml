info:
  name: Transaction start
  type: http
  seq: 2

http:
  method: POST
  url: "{{transaction}}/transactions/{{requestId}}/verify"
  body:
    type: text
    data: "944075"
  auth: inherit

runtime:
  variables:
    - name: requestId
      value: d2bc0381-379b-4a18-97ff-34b962be9b32

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
