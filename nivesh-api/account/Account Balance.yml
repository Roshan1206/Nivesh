info:
  name: Account Balance
  type: http
  seq: 3

http:
  method: GET
  url: "{{account}}/accounts/{{accountId}}/balance"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
