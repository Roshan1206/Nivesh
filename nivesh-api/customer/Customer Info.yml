info:
  name: Customer Info
  type: http
  seq: 2

http:
  method: GET
  url: "{{customer}}/customers/info"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
