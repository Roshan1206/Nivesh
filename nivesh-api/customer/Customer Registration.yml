info:
  name: Customer Registration
  type: http
  seq: 1

http:
  method: POST
  url: "{{customer}}/customers"
  body:
    type: json
    data: |-
      {
        "firstName": "Roshan",
        "middleName": null,
        "lastName": "Sahu",
        "dateOfBirth": "2026-05-30",
        "gender": "male"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
